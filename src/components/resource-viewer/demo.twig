<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, user-scalable=yes">
    <title>Resource viewer demo</title>
    <link rel="stylesheet" href="index.css">
</head>
<body>
<div id="app">
    <demo title="Resource viewer" v-bind:directions="{{ directions|json_encode|escape }}">
        {% for resource in resources %}
            <div slot="content" class="demo-content">
                <h3>{{ resource.title }}</h3>
                <div class="demo-resource-viewer">
                    <demo-resource-viewer inline-template>
                        <resource-viewer v-bind:resource="{{ resource|json_encode|escape }}"
                                         v-bind:devices="{{ devices|json_encode|escape }}"></resource-viewer>
                    </demo-resource-viewer>
                </div>
            </div>
        {% endfor %}
    </demo>
</div>
<script src="index.js"></script>
</body>
</html>
