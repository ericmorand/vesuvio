<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>Prettyprint demo</title>
    <link rel="stylesheet" href="index.css">
</head>
<body>
<div id="app" v-cloak>
    <demo class="demo-theme-prettyprint" title="Prettyprint" v-bind:directions="{{ directions|json_encode|escape }}">
        <div slot="content">
            {% for code in codes %}
                <section class="demo-content">
                    <h3 class="demo-h2">{{ code.title }}</h3>
                    <pre><code class="prettyprint lang-{{ code.lang }}">{{ code.content|escape }}</code></pre>
                </section>
            {% endfor %}
        </div>
    </demo>
</div>
<script src="index.js"></script>
</body>
</html>
